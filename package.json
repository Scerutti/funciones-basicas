{
  "name": "funciones-basicas",
  "version": "1.12.1",
  "description": "Funciones Basicas para proyectos simples",
  "main": "lib/core.js",
  "types": "lib/core.d.ts",
  "module": "lib/core.js",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "lint": "tslint -p tsconfig.build.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm run test && npm run copy:assets",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "react",
    "utilidades"
  ],
  "author": "Sebastián Cerutti",
  "license": "MIT",
  "files": [
    "lib/**/*"
  ],
  "dependencies": {
    "@types/react": "^18.2.25",
    "react": "^18.2.0",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.3",
    "prettier": "^3.0.3",
    "ts-node": "10.9.1",
    "tslint": "6.1.3",
    "tslint-config-prettier": "1.18.0",
    "tslint-react": "4.1.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
